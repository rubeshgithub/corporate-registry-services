/** Shared by the snapshot capture form (checkbox label) and the API (stored
    with the lead as the exact wording the visitor agreed to). */
export const SNAPSHOT_CONSENT_TEXT =
  "Email me occasional filing reminders and offers from Corporate Registry Services. Unsubscribe any time.";
